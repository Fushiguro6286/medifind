node_modules/
.env

# Scrape cache and runtime state. All of it is regenerated by `npm run refresh`.
data/medifind.db
data/medifind.db-wal
data/medifind.db-shm
data/companies/*.json
data/schedule.json
data/raw/
data/uploads/

# data/collectors.json is deliberately NOT ignored: the Scraper Studio collector IDs
# it holds are the submission's proof of Studio usage, and they are what
# `npm run studio:heal` and the /api/provenance trail resolve against.
